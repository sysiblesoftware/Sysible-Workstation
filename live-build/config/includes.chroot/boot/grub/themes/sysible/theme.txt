# Sysible Linux — GRUB theme.
# Dark hex-mesh background, Sysible lockup near the top, and a clean green "pill"
# behind the selected entry (a sliced pixmap, selbar_*.png). Fonts are the GRUB
# default (no .pf2 needed); the layout + selection bar carry the look.

title-text: ""
desktop-image: "background.png"
desktop-color: "#0a0d13"
message-color: "#a7b4cc"
message-bg-color: "#0a0d13"

# Sysible mark + wordmark, centred in the upper third (kept modest so it doesn't
# crowd the menu). logo.png is 3.47:1, so the box keeps that ratio.
+ image {
    file = "logo.png"
    left = 50%-220
    top = 15%
    width = 440
    height = 127
}

# Boot entries — a centred column with a green pill on the selected row.
+ boot_menu {
    left = 30%
    width = 40%
    top = 48%
    height = 34%
    item_color = "#aeb9c9"
    selected_item_color = "#08110a"
    item_height = 44
    item_padding = 14
    item_spacing = 10
    selected_item_pixmap_style = "selbar_*.png"
    scrollbar = false
}

# Footer tagline.
+ label {
    top = 90%
    left = 0
    width = 100%
    align = "center"
    color = "#6f7d94"
    text = "Sysible Linux — engineering & automation workstation"
}
