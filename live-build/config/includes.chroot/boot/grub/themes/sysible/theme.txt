# Sysible Linux — GRUB theme.
# Dark topographic background, Sysible lockup near the top, and a subtle
# translucent card with a thin green left accent behind the selected entry
# (a sliced pixmap, selbar_*.png) — no saturated "pill". Fonts are the GRUB
# default (no .pf2 needed); the layout + selection bar carry the look.

title-text: ""
desktop-image: "background.png"
desktop-color: "#0a0d13"
message-color: "#a7b4cc"
message-bg-color: "#0a0d13"

# Sysible mark + wordmark — sized in PERCENTAGES, not pixels. The live GRUB runs
# at `gfxmode=auto` (a fixed mode stretches under some hypervisors), so the menu
# renders at whatever resolution the firmware reports — anything from 800x600 to
# 4K. A fixed-pixel logo is therefore huge on small modes and tiny on large ones.
# As a percentage the logo is always the SAME fraction of the screen (~24% wide,
# top 13%, bottom ~22%) and the menu sits at top 40%, so they can never overlap
# at any resolution. `left = 38%` centres a 24%-wide box ((100-24)/2 ≈ 38).
+ image {
    file = "logo.png"
    left = 38%
    top = 13%
    width = 24%
    height = 9%
}

# Boot entries — a centred column pulled up right under the logo (no big empty
# gap). Selected row = subtle card + green left bar.
# Geometry note: the selected pill grows by item_padding on every side, so
# item_spacing MUST stay larger than 2*item_padding or the pill bleeds into the
# next entry (that was the old "one green box over both entries" bug). Here
# item_spacing (20) far exceeds 2*item_padding (12) — always one clean row.
+ boot_menu {
    left = 29%
    width = 42%
    top = 40%
    height = 44%
    item_color = "#8a93a0"
    selected_item_color = "#ffffff"
    item_height = 40
    item_padding = 6
    item_spacing = 20
    selected_item_pixmap_style = "selbar_*.png"
    scrollbar = false
}

# Footer tagline.
+ label {
    top = 92%
    left = 0
    width = 100%
    align = "center"
    color = "#6f7d94"
    text = "Sysible Linux — engineering & automation workstation"
}
