# Sysible Workstation password-quality floor, enforced via pam_pwquality
# (libpam-pwquality) on `passwd` and account creation. Moderate, not draconian —
# a network-reachable workstation should not accept trivial passwords.
minlen = 10
minclass = 3
maxrepeat = 3
gecoscheck = 1
enforcing = 1
